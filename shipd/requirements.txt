# Shipd backport review-priority ranking -- v2 solution.py
# solution.py imports ONLY: sys, pathlib (stdlib) + numpy, pandas.
# No LightGBM, scikit-learn, torch or network access is required or used.
numpy>=1.24
pandas>=2.0
